<h3 className="text-lg font-semibold text-gray-900 dark:text-white mb-4">Dados de Identificação</h3>
                  <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
                    <div>
                      <label className="block text-sm font-medium text-gray-700 mb-2">Nome Completo</label>
                      <input
                        type="text"
                        value={paciente.dadosIdentificacao?.nomeCompleto || ''}
                          });
                        }}
                        className={`w-full border border-gray-300 rounded-md px-3 py-2 text-gray-900 `}
                      />
                    </div>
                    <div>
                      <label className="block text-sm font-medium text-gray-700 mb-2">Email</label>
                      <input
                        type="email"
                        value={paciente.dadosIdentificacao?.email || ''}
                          });
                        }}
                        className={`w-full border border-gray-300 rounded-md px-3 py-2 text-gray-900 `}
                      />
                    </div>
                    <div>
                      <label className="block text-sm font-medium text-gray-700 mb-2">Telefone</label>
                      <input
                        type="tel"
                        value={paciente.dadosIdentificacao?.telefone || ''}
                          });
                        }}
                        className={`w-full border border-gray-300 rounded-md px-3 py-2 text-gray-900 `}
                      />
                    </div>
                    <div>
                      <label className="block text-sm font-medium text-gray-700 mb-2">CPF</label>
                      <input readOnly disabled
                        type="text"
                        value={paciente.dadosIdentificacao?.cpf || ''}
                          });
                        }}
                        className="w-full border border-gray-300 rounded-md px-3 py-2 text-gray-600 bg-gray-50"
                      />
                    </div>
                    <div>
                      <label className="block text-sm font-medium text-gray-700 mb-2">Data de Nascimento</label>
                      <input readOnly disabled
                        type="date"
                        value={(() => {
                          const date = paciente.dadosIdentificacao?.dataNascimento;
                          if (!date) return '';
                          try {
                            const d = new Date(date);
                            if (!isNaN(d.getTime())) {
                              // Formatar data no horário local para evitar problemas de fuso horário
                              const year = d.getFullYear();
                              const month = String(d.getMonth() + 1).padStart(2, '0');
                              const day = String(d.getDate()).padStart(2, '0');
                              return `${year}-${month}-${day}`;
                            }
                            return '';
                          } catch {
                            return '';
                          }
                        })()}
                          setPacienteEditando({
                            ...paciente,
                            dadosIdentificacao: {
                              ...paciente.dadosIdentificacao,
                              dataNascimento: dataNascimento
                            }
                          });
                        }}
                        className="w-full border border-gray-300 rounded-md px-3 py-2 text-gray-600 bg-gray-50"
                      />
                    </div>
                    <div>
                      <label className="block text-sm font-medium text-gray-700 mb-2">Sexo Biológico</label>
                      <select
                        value={paciente.dadosIdentificacao?.sexoBiologico || ''}
                          });
                        }}
                        className="w-full border border-gray-300 rounded-md px-3 py-2 text-gray-600 bg-gray-50"
                       disabled>
                        <option value="">Selecione</option>
                        <option value="M">Masculino</option>
                        <option value="F">Feminino</option>
                        <option value="Outro">Outro</option>
                      </select>
                    </div>
                    
                    {/* CEP */}
                    <div>
                      <label className="block text-sm font-medium text-gray-700 mb-2">CEP</label>
                      <div className="flex gap-2">
                        <input readOnly disabled
                          type="text"
                          value={paciente.dadosIdentificacao?.endereco?.cep || ''}
                              }
                            });
                          }}
                                    }
                                  });
                                }
                              } catch (error) {
                                console.error('Erro ao buscar CEP:', error);
                              }
                            }
                          }}
                          className="flex-1 border border-gray-300 rounded-md px-3 py-2 text-gray-900"
                          placeholder="Ex: 12345678"
                          maxLength={9}
                        />
          
                      </div>
                    </div>

                    {/* Endereço - Rua */}
                    <div className="md:col-span-2">
                      <label className="block text-sm font-medium text-gray-700 mb-2">Endereço (Rua)</label>
                      <input readOnly disabled
                        type="text"
                        value={paciente.dadosIdentificacao?.endereco?.rua || ''}
                            }
                          });
                        }}
                        className="w-full border border-gray-300 rounded-md px-3 py-2 text-gray-600 bg-gray-50"
                        placeholder="Ex: Rua Exemplo, 123"
                      />
                    </div>

                    {/* Cidade e Estado */}
                    <div>
                      <label className="block text-sm font-medium text-gray-700 mb-2">Cidade</label>
                      <input readOnly disabled
                        type="text"
                        value={paciente.dadosIdentificacao?.endereco?.cidade || ''}
                            }
                          });
                        }}
                        className="w-full border border-gray-300 rounded-md px-3 py-2 text-gray-600 bg-gray-50"
                        placeholder="Ex: São Paulo"
                      />
                    </div>

                    <div>
                      <label className="block text-sm font-medium text-gray-700 mb-2">Estado</label>
                      <input readOnly disabled
                        type="text"
                        value={paciente.dadosIdentificacao?.endereco?.estado || ''}
                            }
                          });
                        }}
                        className="w-full border border-gray-300 rounded-md px-3 py-2 text-gray-600 bg-gray-50"
                        placeholder="Ex: SP"
                      />
                    </div>

                    {/* Data de Cadastro e Médico Responsável */}
                    <div>
                      <label className="block text-sm font-medium text-gray-700 mb-2">Data de Cadastro</label>
                      <input readOnly disabled
                        type="text"
                        value={(() => {
                          const date = paciente.dadosIdentificacao?.dataCadastro;
                          if (!date) return '';
                          try {
                            const d = new Date(date);
                            if (!isNaN(d.getTime())) {
                              return d.toLocaleDateString('pt-BR');
                            }
                            return '';
                          } catch {
                            return '';
                          }
                        })()}
                        readOnly
                        className="w-full border border-gray-300 rounded-md px-3 py-2 text-gray-600 bg-gray-50"
                      />
                    </div>

                    <div>
                      <label className="block text-sm font-medium text-gray-700 mb-2">Médico Responsável</label>
                      <input readOnly disabled
                        type="text"
                        value={(() => {
                          const medico = medicoPerfil;
                          if (!medico) return '';
                          const titulo = medico.genero === 'F' ? 'Dra.' : 'Dr.';
                          return `${titulo} ${medico.nome}`;
                        })()}
                        readOnly
                        className="w-full border border-gray-300 rounded-md px-3 py-2 text-gray-600 bg-gray-50"
                      />
